[build]
builder = "NIXPACKS"

[deploy]
startCommand = "python -m uvicorn luthien_control.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10